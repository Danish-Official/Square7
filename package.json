{
  "scripts": {
    "devServer": "cd ./backend && npm run server",
    "server": "cd ./backend && node server.js",
    "client": "cd ./frontend && npm run dev",
    "start": "concurrently \"npm run devServer\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
